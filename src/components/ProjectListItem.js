import React from 'react';

const ProjectListItem = () => {
	// should receive props just for individual project
	// with its items


	return (
		<p>this is a sample project tiles</p>
	);
}

export default ProjectListItem;